# Tinymfv Alignment

## Goal
Align training prompts and fixed interview probes with `tinymfv`-style moral judgment, then make the harness scientifically self-diagnosing: every prompt-gym, fake-student, and real 2B run must emit mandatory teacher feedback, fail early on generic candidate pools, and leave enough quoted evidence to judge whether the run tells a coherent causal story.

## Scope
In: simplify the prompt pool toward `tiny-mfv scifi` judgment prompts, simplify fixed pre/post probes toward `tinymfv`-style classic `_1p/_3p` pairs with short followups, remove free-text axis writing from weak selection, make `harness_feedback` mandatory and reusable, fail early on generic candidate pools, audit prompt-gym and the real 2B run, restore the staged dogfood path, add a deterministic selection lesion that removes weak-teacher ranking, then rerun a real 2B path and classic eval.
Out: changing adapter math, changing teacher model, adding backward compatibility.

## Requirements
- R1: Training pool contains `tiny-mfv scifi` judgment-style prompts that afford short committed judgments plus brief reasoning. Done means: rebuilt `src/csm/gen/pool.jsonl` contains new `tiny-mfv-scifi` rows with clear source/config/tags. VERIFY: grep the pool and inspect representative rows. If it silently failed, pool composition would remain daily-dilemmas-heavy and no scifi rows would appear.
- R2: Fixed interview probes use `tinymfv`-style `_1p/_3p` situations while preserving the `_1p/_3p` structure expected by teacher scoring. Done means: `interview_pre.json` still has paired `_1p`/`_3p` ids, but the openings are short judgment prompts rather than bespoke essay dilemmas. VERIFY: run smoke and inspect `interview_pre.json`. If it silently failed, old `equity_split/growth_deck/burn_bridges` probes would remain.
- R3: The harness still runs end-to-end after the simplification. Done means: smoke writes `interview_pre.json`, `candidates.json`, `pairs.md`, `judgment.json` without runtime errors. VERIFY: run the fast smoke target and inspect the round artifacts. If it silently failed, smoke would exit nonzero or miss key artifacts.
- R4: Weak selection no longer asks the teacher for a free-text axis that the measured persona-cell library cannot instantiate. Done means: the teacher picks a measured persona pair id, and candidate generation only uses cells from that pair. VERIFY: inspect `candidates.json` and the choose-focus interface text. If it silently failed, the teacher could still request `honesty` and receive only `wellbeing_authority` candidates.
- R5: Manual multi-stage audit and teacherless lesion both use the same measured-pair interface as the main harness. Done means: `scripts/dogfood_round.py` no longer calls the removed `axis=` interface, and can auto-select surviving pairs without the weak teacher. VERIFY: run `choose` then `auto-select` on a staged round. If it silently failed, the dogfood path would drift from the live harness or crash on the old signature.
- R6: Every judged round records non-empty `harness_feedback`, and the next round's teacher prompt includes it. Done means: `judgment.json` always contains `harness_feedback`, and `teacher_prompt.md` for the next round quotes the prior feedback. VERIFY: inspect a replay or fake-student run across two rounds. If it silently failed, the drop reason might be preserved but the teacher would not see the prior harness criticism.
- R7: Generic or replay-mismatched candidate pools fail before selection with an explicit reason. Done means: `choose_focus` raises a genericity- or replay-specific validation error and leaves the round in `choose_focus`. VERIFY: inspect `candidates.json` plus the teacher log. If it silently failed, the teacher would still grind through junk or impossible selection quotas.
- R8: The real 2B debug profile only trains on enough selected pairs and only counts as a learning step if held-out val improves beyond step 0. Done means: a real `qwen-2b-3keep` round reaches `>=10` selected pairs, produces a non-generic `selected_pair_review.md`, and `train_summary` shows `best_step > 0` with `val_improvement >= min_val_improvement`. VERIFY: inspect `selected_pair_review.md` and `calibration.json`. If it silently failed, the run might report training success while actually memorizing or learning from too few pairs.
- R9: At least one run gets a detailed quoted audit that answers whether the story is coherent and compelling. Done means: an `audit.md` quotes teacher input/output, selected pairs, train-vs-val, student pre/post outputs, and final judgment, then makes an explicit verdict. VERIFY: read the report and check that each claim is grounded in a local artifact quote. If it silently failed, the report would summarize vibes without enough evidence to falsify itself.
- R10: The post-fix claim must be shown on a full 3-round run, not a single improved round00. Done means: one fresh `qwen-2b-3keep` slug completes three rounds, `csm eval` artifacts exist for that slug, and the audit traces the causal story across all completed rounds. VERIFY: inspect the slug for `round00`, `round01`, `round02`, top-level `index.html`, and quoted audit coverage of each round. If it silently failed, we would over-read one locally improved round while the longer trajectory still collapses or stalls.

## Tasks
- [x] T1 (R1): Replace or heavily downweight the current character pool with `tiny-mfv scifi` judgment prompts.
  - steps: update `scripts/build_pool.py`, rebuild `src/csm/gen/pool.jsonl` and manifest
  - verify: `rg -n 'tiny-mfv|scifi|How wrong is it|What should they have done instead' src/csm/gen/pool.jsonl`
  - success: pool contains judgment-style scifi rows
  - likely_fail: eval-leak guard strips the new rows by mistake
  - sneaky_fail: rows are present but still overly essay-like / not committed enough
  - UAT: "when I inspect the pool, I see scifi moral-judgment prompts rather than mostly daily dilemmas"
- [/] T2 (R2, R4): Rewrite `src/csm/gen/probes.py` and weak-select interface around one measured `tinymfv` format.
  - steps: keep `_1p/_3p` ids and paired stems, replace openings/followups, remove free-text axis from `choose_focus`, constrain candidate generation to the selected measured pair
  - verify: `uv run python - <<'PY' ... probe ids/openings ... PY`
  - success: probes are short judgment-first, then brief reasoning, and the teacher selects from measured pairs rather than inventing an axis
  - likely_fail: teacher formatting assumes old probe ids / old choose_focus signature and breaks
  - sneaky_fail: the teacher still chooses a pair label that is not what candidate generation actually uses
  - UAT: "when I inspect interview_pre and candidates.json, the probes look like tinymfv-style judgment tasks and the persona pair is explicit"
- [x] T3 (R3): Smoke the harness and inspect the new artifacts.
  - steps: run the repo smoke config, inspect round artifacts
  - verify: `just smoke` with full log saved, then inspect `out/.../round00/interview_pre.json`
  - success: smoke green and artifacts reflect the new probe/pool style
  - likely_fail: build_pool or probe imports break runtime
  - sneaky_fail: smoke passes but artifacts still show old prompts
  - UAT: "when I open the smoke artifacts, I can see the new scifi judgment prompts in both pool/interview outputs"
- [ ] T4: Prompt-gym and fresh-eyes review, then real 2B rerun and classic eval.
  - steps: run `just smoke-prompts 1`, audit the teacher's choices, then rerun a real 2B profile and `csm eval --name classic`
  - verify: `git diff --stat` and artifact links
  - success: one coherent commit with proof
  - likely_fail: prompt gym still uses stale fixtures or the teacher still selects off-axis junk
  - sneaky_fail: a real run completes, but `classic` movement is still noise because the selected pair axis is wrong or the model just learned format
  - UAT: "the commit plus linked artifacts show unified prompts, measured-pair selection, and a real classic eval result"
- [ ] T5 (R5): Restore staged dogfood and add deterministic auto-select for the selection lesion.
  - steps: patch `scripts/dogfood_round.py` to take `persona_pair_id`, then add `auto-select` that picks the highest-score surviving candidate per scenario
  - verify: run a staged `init tiny` -> `choose` -> `auto-select` -> `train` flow on a fake-student round
  - success: the manual path matches the live measured-pair interface, and we can run a deterministic selector without editing the core harness
  - likely_fail: the helper still calls removed `axis=` arguments and crashes
  - sneaky_fail: `auto-select` works but picks pruned candidates or uses a different survivor definition than the live harness
  - UAT: "when I dogfood the round stage by stage, both manual selection and deterministic auto-selection operate on the same candidate pool as the main harness"
- [x] T6 (R6, R7): Make harness feedback mandatory and fail fast on generic candidate pools.
  - steps: require non-empty `harness_feedback` in `mark_exam`, carry it into `teacher_prompt.md`, add genericity detection in `choose_focus`, make replay/profile mismatches fail loudly
  - verify: run replay prompt-gym and fake-student prompt-gym; inspect `judgment.json`, `teacher_prompt.md`, and `candidates.json`
  - success: every judged round has feedback, and generic pools stop before selection
  - likely_fail: auto-drop paths skip `harness_feedback`
  - sneaky_fail: `teacher_prompt.md` exists but omits the prior feedback text the teacher needs
  - UAT: "when a round drops, I can read the criticism in `judgment.json` and see it fed into the next round prompt; when the pool is generic, the round dies in `choose_focus` for that reason"
- [/] T7 (R8, R9): Run and audit a real `qwen-2b-3keep` round under the new fail-fast rules.
  - steps: queue the real run with OpenRouter env, inspect selected-pair count and quality, inspect `train_summary`, then write a quoted audit report
  - verify: `pueue log 24 -l 120`, then inspect the newest `out/iter/*_iter_qwen-qwen3.5-2b/round00/{selected_pair_review.md,calibration.json,judgment.json}` and `audit.md`
  - success: either the run satisfies the training gate honestly or the audit makes the failure causal chain explicit
  - likely_fail: the teacher still cannot reach `>=10` good pairs, so the round drops before or during training
  - sneaky_fail: training technically passes, but the selected pairs are still semantically inverted and the story is not compelling
  - UAT: "when I open the run artifacts, I can see whether the teacher had enough good pairs, whether val improved for real, and whether the run tells a coherent causal story"
- [/] T8 (R10): Run a fresh full 3-round `qwen-2b-3keep`, run `csm eval`, and write a multi-round quoted audit.
  - steps: inspect the current profile for blockers, queue a fresh post-fix run and let it finish all three rounds, run `csm eval --slug <slug>`, then write an audit that traces PRE, choose_focus, selected pairs, train/val, calibration, POST, and judgment for each completed round
  - verify: `pueue log <task> --full`, then inspect `out/iter/<slug>/round0{0,1,2}/{judgment.json,selected_pair_review.md}` plus top-level `index.html` and `audit.md`
  - success: one slug contains three completed rounds and enough quoted evidence to judge the full trajectory rather than a single round
  - likely_fail: round01 or round02 collapses back to too few pairs or step-0-best training, so the longer run is not actually better than the single-round milestone
  - sneaky_fail: three rounds complete, but `csm eval` is missing or the audit handwaves the later rounds instead of tracing them concretely
  - UAT: "when I open the final slug, I can read three rounds of artifacts plus a single audit that tells me whether the whole run actually worked"

## Context
- Current mismatch: training pool is mostly daily-dilemmas action prompts, eval is `tinymfv classic`.
- Existing teacher/judgment code expects `_1p/_3p` paired ids and scores every `_1p` seat.
- `tiny-mfv` configs available locally: `classic`, `scifi`, `ai-actor`.
- Audit of `out/iter/20260613T233435_iter_qwen-qwen3.5-2b/round00` showed the weak teacher selected obviously bad pairs and was forced to choose under axis `acted honesty vs. strategic optimization` while the only offered measured cells were `wellbeing_authority`. That mismatch is a harness bug, not a weak-teacher success/failure.

## Log
- 2026-06-14: User pushed toward using `tiny-mfv` for both generation and interviews, with short committed judgment followed by reasoning.
- 2026-06-14: Fresh-eyes GPT-5.5 review recommended a simpler path: single measured pair first, held-out `classic` probes, and no free-text axis unless the library truly spans it.
- 2026-06-14: `just smoke` passed after switching probes to held-out `classic` vignettes, switching fake-student gym probes to dynamic payloads, and changing `choose_focus` to select `persona_pair_id` instead of a free-text axis.
- 2026-06-14: GPT-5.5 plan review said the current checks still mostly prove wiring, not scientific separation; strongest next lesion is a deterministic selector on the same prompt/persona path that removes weak-teacher ranking.
- 2026-06-14: Real 2B audit showed the measured-pair interface works, but selection quality is still weak: in round00, scenarios 1 and 2 had only one surviving candidate each, and those survivors were semantically bad or inverted. That is upstream candidate-quality failure plus weak drop discipline, not evidence that the selector works.
- 2026-06-14: Fresh prompt-gym rerun still drops correctly on generic boilerplate. The weak teacher now uses the interface as intended, but the fake-student generator remains too formulaic to test semantic discrimination.
- 2026-06-14: Made `harness_feedback` mandatory, carried it into the next round prompt, added replay-candidate support to prompt-gym, and added a genericity failure gate in `choose_focus`. Replay prompt-gym now tests the teacher against real candidate pools rather than only synthetic boilerplate.
- 2026-06-14: Queued a real `qwen-2b-3keep` run as pueue task 24 using `scripts/run_with_tinymfv_env.sh` so the OpenRouter env survives the queue shell.
- 2026-06-14: Live task 24 reached `train_student` on round00 with selected autonomy/coercion pairs. Early train signal looked modestly better than the earlier toy failures: `train nll+ 2.19 -> 1.47`, `val nll+ 2.17 -> 2.09`, early-stop at step 9 rather than step 0. Need the full artifacts and quoted outputs before calling this progress real.
- 2026-06-14: Full audit of task 24 / slug `20260614T035645_iter_qwen-qwen3.5-2b` showed the deeper failure: the round reached 10 selected pairs and cleared the mechanical train gate, but at least 3 selected `How wrong is it` Cho answers were visibly wrong-pole (`1` / `Rating: 1`) and one selected pair was blurred with both poles protecting autonomy. The final teacher prose also mixed surface wrongness ratings with axis scores, so the narrative and stored `pre_scores` / `post_scores` disagreed.
- 2026-06-14: Added a fail-fast semantic lint for `How wrong is it` prompts in `_candidate_flags`: parse the leading rating, prune Cho answers with no leading rating, prune Cho answers at `1` or `2`, and prune candidates where Cho is not more condemning than Rej. Added a focused pytest for that exact bad pattern.
- 2026-06-14: Re-queued the real 2B run as pueue task 25 to verify the new wrongness-sign lint improves selected-pair quality instead of merely moving the failure downstream.
- 2026-06-14: In task 25, the teacher no longer chose `autonomy_coercion`; it chose `fairness_integrity`, reading the PRE gap as "knows cheating is wrong, but defaults to punitive enforcement rather than fair human handling." This is a meaningful branch in the experiment, not just a rerun of the old axis.
- 2026-06-14: First `Cho` sample under `fairness_integrity` already shows a likely new confound: `acts fairly and keeps the process honest` drifted into `respect for their superior's authority during an important ceremony`. If the full selected pool repeats that pattern, the next fix is not the wrongness-sign lint but a measured-template/axis mismatch for fairness.
- 2026-06-14: Full task-25 audit showed the stronger diagnosis: the fairness run was still sampling `character` prompts by family only, so `fairness_integrity` saw many authority/loyalty rows. A GPT-5.5 subagent review judged the root cause mostly prompt mismatch, not template failure. Patched `rows_for_family` / `sample_prompt_rows` / `choose_focus` to require pair-compatible prompt axes (`fairness_integrity -> fairness|honesty`, `autonomy_coercion -> autonomy`, `wellbeing_authority -> care|authority`) and tightened the fairness descriptors toward favoritism/deniability instead of vague "process". Queued rerun task 26 to test whether this yields cleaner selected pairs and a real val improvement.
- 2026-06-14: Task 26 / slug `20260614T042427_iter_qwen-qwen3.5-2b` cleared the round00 mechanical gate honestly after the axis-filter fix: 12 selected pairs, best held-out `val nll+` improved from `1.98` to `1.90` at step 11, and the teacher kept the round with `movement_mean = 0.17`. But the user correctly objected that this is still only one completed round, and the story is not strong enough without a full 3-round rerun plus post-hoc eval.
- 2026-06-14: Rechecked the live 3-round profile before relaunch: `qwen-2b-3keep` still targets `n_rounds=3`, `n_scenarios=36`, `n_headroom_prompts=20`, `n_candidate_pairs=5`, `min_pairs_to_train=10`, and `min_val_improvement=0.05`. Focused regression pytest for the wrongness-sign and required-axes guards passed (`2 passed, 1 deselected`), so the next honest step is a fresh full 3-round rerun rather than more local patching.
- 2026-06-14: Full task-27 slug `20260614T051341_iter_qwen-qwen3.5-2b` reached train on round00 and failed honestly at held-out val (`train nll+ 2.14 -> 1.45`, `val nll+ 2.02 -> 2.04`, best step 0). The selected-pair review showed the deeper issue: the teacher called the pool "structurally clean" even though several chosen pairs were visibly dirty or inverted. This strengthens the case that freeform selection is too soft.
- 2026-06-14: Patched `select_pairs` so it now requires structured teacher judgments per chosen pair (`candidate_id`, `on_axis_forward`, `on_axis_reverse`, `off_axis_clean`, `comment`) and rejects weak picks before train. `read_candidate` now shows the pair in both orders and states the omit threshold explicitly. Focused pytest still passes (`3 passed, 1 deselected`).
- 2026-06-14: Prompt-gym on the fake student still dies at the genericity gate before selection, which is expected. Replay prompt-gym on real round00 candidates shows the new selector is doing useful work: the weak teacher no longer glides past dirty autonomy pairs, and repeated tool rejections force it to confront mis-signed or muddy candidates. It is still not graceful, but it is much harder for the teacher to smuggle bad pairs into train unnoticed.
- 2026-06-14: Full 3-round rerun task 28 showed the next bottleneck more clearly. The weak teacher could reason about pair quality, but it kept tripping over `scenario_id` plus `candidate_id` bookkeeping and repeatedly selected pruned rows. That looks more like an interface bug than a pure weak-model failure.
- 2026-06-14: Simplified selection to use survivor handles only. `choose_focus` now prints survivors as handles like `s6c1`, `read_candidate` only accepts a surviving handle and refuses pruned rows, and `select_pairs` now takes a list of structured survivor judgments instead of a map keyed by scenario id. This is a breaking change on purpose: the teacher should never need to reason about non-selectable candidate ids.
- 2026-06-14: Verified the survivor-handle interface three ways before requeueing a real run: focused pytest passed (`3 passed, 1 deselected`), `scripts/smoke.sh` passed end-to-end on replay, and prompt-gym replay on a real round used `read_candidate(survivor_id=...)` correctly and reached `select_pairs` with the new list-shaped payload.
- 2026-06-14: Fresh real run `20260614T061805_iter_qwen-qwen3.5-2b` proved the next bottleneck cleanly. Round00 no longer failed on bookkeeping; it selected 10 survivors and reached train. But the teacher's own drop reason was right: `autonomy_coercion` under `character` sampled 15 rows that were only 5 underlying worlds repeated in 3 judgment forms each (Hutt slaver, chained elemental, silenced bard, bound djinn, guild formula leak). Val improvement was only `0.010`, below the required `0.050`.
- 2026-06-14: Patched `sample_prompt_rows` to cap repeated world-events by normalized scene stem during round sampling. For the autonomy bank this yields all 15 available rows with at most 3 variants per world instead of over-sampling one or two worlds. Focused pytest passes, and a direct check shows `max_per_stem = 3`.
- 2026-06-14: Real run `20260614T070545_iter_qwen-qwen3.5-2b` on `wellbeing_authority` finally looked like a pure semantic bottleneck rather than harness/UI friction. The teacher used the new rate-then-select flow correctly and reached 8 passing survivors across 8 unique scenarios, then stalled below the `>=10` floor. Two of the "remaining" survivors were obviously poison and should never have been presented: `s3c5` contained `REJ: "I am not an AI..."`, and `s6c2` contained `REJ: "I cannot roleplay personas..."`, yet both had `flags: []`. That is a concrete pruning bug, not weak-teacher failure.
- 2026-06-14: Patched candidate generation to re-enable the refusal bad-words ban for `cho` and `rej` candidate sampling, and widened `_CHARACTER_BREAK_PHRASES` to catch the exact failure forms from task 33 (`i am not an ai`, `i do not have feelings`, `i cannot roleplay`, `safety guidelines`, `i cannot evaluate this situation`). Focused pytest passed (`5 passed, 3 deselected`), replaying the exact task-33 survivors now yields `['character_break_rej']`, and `scripts/smoke.sh` still passes.
- 2026-06-14: Real run `20260614T071755_iter_qwen-qwen3.5-2b` showed the next harness/UI bug cleanly after the refusal fix. The candidate pool improved to 23 survivors across 13 scenarios with zero kept `character_break` rows, but the weak teacher still failed at selection because it had to remember which passing survivors were duplicates of the same scenario. It first tried to submit after only 3 passes, then after more rating it built 12 passing survivors across 9 unique scenarios and still submitted duplicates like `s4c2` + `s4c4` and `s8c1` + `s8c5`.
- 2026-06-14: Patched `rate_candidate` so the harness, not the teacher, now keeps only the strongest passing survivor per scenario in the live shortlist. A stronger pass supersedes the prior one and demotes it out of the shortlist. `rate_candidate` now reports the deduplicated shortlist directly. Focused pytest passed (`4 passed, 5 deselected`), including a new regression that a stronger `s1c2` supersedes an earlier `s1c1` from the same scenario.
- 2026-06-14: Real run `20260614T072944_iter_qwen-qwen3.5-2b` showed the next remaining weak-teacher failure after shortlist dedup: the teacher now had the correct shortlist, but still kept trying `select_pairs(...)` too early at 5/10 because the tool surface did not make the remaining gap explicit enough. Patched `rate_candidate` to report `Need N more unique scenarios to reach <min_to_train>`, and patched the `select_pairs` validation error to include the current passing shortlist and exact remaining count. Focused pytest passed (`5 passed, 5 deselected`).
- 2026-06-14: Real run `20260614T073709_iter_qwen-qwen3.5-2b` finally isolated a generation-side failure rather than more selector friction. Round00 kept honestly, but round01 produced zero passing survivors because the weak teacher repeatedly rated REJ as still too wellbeing-adjacent. The consistent complaint was that REJ spoke in humane language instead of actually paying the authority-vs-wellbeing cost. Patched measured-pair candidate prompting to append a one-line pair-specific behavior gloss for each pole, so the negative pole must explicitly instantiate the failure mode (`preserve order even when that allows harm`, `pressure or overrule for control`, `bend the standard for insiders`) instead of relying on the bare descriptor alone. Focused pytest passed and replay smoke still passed.
- 2026-06-14: Real run `20260614T084835_iter_qwen-qwen3.5-2b` exposed the deeper prompt-family bug. The softened gloss fixed the positive pole's meta-parsing, but the first negative sample for `autonomy_coercion` still answered the underlying moral-repair question, "The actor should have instead respected elemental freedom...", instead of inhabiting coercion. That means `What should the actor have done instead?` itself is biasing both poles toward improvement. Patched sampled training prompts and fixed `_3p` interview probes to use direct action, `What does the actor do next?`, while keeping the short wrongness prompt unchanged. Hypothesis: this preserves open-ended behavior but finally lets the negative pole act badly enough to train against.
- 2026-06-14: Fresh rerun `20260614T085512_iter_qwen-qwen3.5-2b` immediately exposed a second routing bug: the teacher chose `wellbeing_authority`, but the first positive sample landed on a pure authority-etiquette scene, "boots up on the war-table," and answered `1 (Not Wrong)`. That is not a bad generation, it is the wrong training conflict. Tightened `PAIR_REQUIRED_AXES["wellbeing_authority"]` from `("care", "authority")` to `("care",)` so this axis trains on harm/welfare scenes rather than simple protocol disrespect.
- 2026-06-14: After the focus-choice fix, task `20260614T090442_iter_qwen-qwen3.5-2b` finally chose `autonomy_coercion`, but the first positive sample still hit the old wrongness-format pathology: `1 (Not Wrong)` followed by a morally condemning explanation. That confirmed the wrongness prompt family is still poisoning candidate generation even when axis choice is right. Patched `training_prompt_surface(...)` so sampled training rows rewrite `How wrong is it...` to the concern prompt, while fixed probes keep the original wrongness item for measurement. Training generation now uses only concern + direct-action surfaces on those worlds.
- 2026-06-14: Task `20260614T090824_iter_qwen-qwen3.5-2b` was the first run that picked a live mismatch for the right reason, but the first fairness `Cho` still came back semantically dirty: it admitted manipulation "to protect her favored player" instead of publicly restoring the same standard. The paired `Rej` was much cleaner and already did insider favoritism plus deniability. Tightened the `fairness_integrity` behavior glosses so the positive pole now explicitly says `undo the rigging in public and apply the same standard to both sides`, while the negative pole says `quietly rig ... and cover it as luck, procedure, or mistake`.
- 2026-06-14: Real run `20260614T091238_iter_qwen-qwen3.5-2b` finally reached `train_student` on `autonomy_coercion` with 10 selected scenarios, but it failed the held-out transfer gate honestly: `val nll+ 2.64 -> 2.62`, only `0.022` better than step 0, below the required `0.050`. The clearest selector bug in the chosen bank was `s12c1`: the teacher's own comment said `Rej misattribution: claims the djinn is coercing rather than the sorcerer coercing the djinn`, yet the pair still passed because the numeric thresholds alone allowed it. Hardened the selector so comments that explicitly admit actor-role inversion or misattribution can never pass, and updated the teacher brief to say wrong actor/wrong victim is a fatal pair failure, not a mild confound.
- 2026-06-14: Prompt-gym after the actor-role inversion patch still behaved as intended: the fake-student run never reached selection and died at `choose_focus` for the existing genericity gate, not because the new selector wording broke the tool path. The weak teacher exhausted the allowed family/pair combinations and repeatedly got `generic candidate pool` with the same boilerplate signatures, which is the expected nuisance-path check for this fake branch.
- 2026-06-14: Real run `20260614T093307_iter_qwen-qwen3.5-2b` showed the next more scientific failure. Round00 passed the held-out train gate honestly (`val nll+ 2.86 -> 2.75`) and no selected pair passed with a self-admitted misattribution comment, so the actor-role inversion fix mattered. But `mark_exam` still dropped the round because movement appeared in the wrong place: the selected `autonomy_coercion` bank produced no `_1p` movement on the autonomy seat, and the teacher only saw useful change in the wellbeing-adjacent material. Inspecting the autonomy bank showed the culprit: 3 of its 15 rows were tagged `['autonomy', 'power']`, mainly the Hutt/slavery scenes that teach vivid harm/power conflict rather than clean consent/coercion. Patched `rows_for_family` / `sample_prompt_rows` / `choose_focus` to support `forbidden_axes`, and set `PAIR_FORBIDDEN_AXES['autonomy_coercion'] = ('power',)`. The resulting autonomy pool is now 12 rows: pure autonomy plus autonomy+loyalty, with autonomy+power excluded.
- 2026-06-14: Before task 50 starts, one more concrete nuisance surfaced in the current autonomy artifacts. The selected survivors in `20260614T093307_iter_qwen-qwen3.5-2b` still contain many 60+ token punctuation-free rambles, and the weak teacher sometimes overrates them because they mention the right stake words. Patched `_degenerate_gen` to cull long no-punctuation runs, and added a focused regression. Prompt-gym after this gate change still failed only on the expected fake-student genericity path: `20260614T095514_iter_wassname-qwen3-5lyr-tiny-random` dropped with `generic candidate pool` boilerplate rather than a regression in selection or state flow.

## TODO
- Add multi-dataset post-hoc eval (`classic` + `scifi` + `ai-actor`) in one report if this simplification works.
- Expand the measured persona-pair library so the weak teacher can choose among several genuinely different axes rather than one default pair.

## Errors
| Task | Error | Resolution |
|------|-------|------------|
| T3 | `just smoke` bypassed `prepare_round` by writing an empty `interview_pre.json`, so it could not validate the new probe interface. | Patched `scripts/smoke.sh` to call `prepare_round` and updated the smoke expectations to the new probe ids and `choose_focus(persona_pair_id, ...)` contract. |
| T4 | `just smoke-prompts` failed before the teacher ran because the OpenRouter key was not in env. | Reran with `/media/wassname/SGIronWolf/projects5/2026/lite/tinymfv/.env` sourced so the prompt-gym artifact reflects the actual brief rather than an env misconfiguration. |
| T7 | Queued shell did not preserve the OpenRouter env reliably. | Added `scripts/run_with_tinymfv_env.sh` and used it in pueue task 24 so the real run sees the same env as manual shells. |
