---
name: rej-collapse-is-composition
description: "the rej-pole gen collapse is composition x extreme-neg, not persona framing"
metadata: 
  node_type: memory
  type: project
  originSessionId: 112c7515-54ff-4bb3-9ca6-fdbb6b24acf3
---

The #1 keep-rate killer (rej-pole gen degenerating into "of of of search" loops +
multilingual sprays) is an INTERACTION, not a bad persona string. Real-student
probe (scripts/probe_persona_coherence.py, pueue 47/48): on BASE every neg
framing is coherent, including the exact abstract-system text that collapsed in
task-46 r02. It only collapses under COMPOSITION (a kept adapter baked into W),
because the merged weights steer toward the character while the neg pole asks for
the militant opposite. Asymmetry confirms it: only the rej pole (opposing the
baked direction) collapses; the cho pole (aligned) and the aligned POS_ref stay
clean. Controlled triple: extreme-neg+base=fine, extreme-neg+baked=degrades,
aligned+baked=fine. That is why r00 (no history baked) was the one keep and
r01/r02/r03 (all composed) all dropped with degenerate rej.

So "just use role-play framing" (pretend-you-are / act-as-if / ai-assistant) is
NOT the fix -- framing was a weak, inconsistent lever (ai-assistant framing was
worst). Better-supported directions: (1) make the neg pole a SUBTLE lean, not a
cartoon anti-character -- extremity both maximally opposes the baked weights AND
is the comply-vs-refuse axis the brief warns against; (2) add a degeneracy
detector to the pair audit (reuse the canary d3+non-ascii) that culls collapsed
pairs and keeps the round in propose_personas so the teacher can see+retry
(currently blind: audit only flags blur/skew, and state railroads to
train_student). See RESEARCH_JOURNAL 2026-06-04 (c). Relates to
[[probe-for-wisdom-not-action]].
